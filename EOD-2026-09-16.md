EOD: GroundStandard: gsTeam Scoreboard v2

1. Made the clone of v1 — schema, 1,945 rows, 32 tables, logins, realtime, edge functions. Kept on disk with a restore script.
2. Renamed the dashboard to CA Rollup. Nothing else on that screen changed.
3. Built the Leads section — totals, funnel, and the source split: Facebook, Google, website, phone.
4. Built the Ads section — per platform, by campaign and ad set. Spend, clicks, CTR, leads, cost per lead.
5. Both are empty live because the syncs do not exist yet. They say so instead of showing zeros.
6. Added demo fixtures so the screens can be shown to Bobby now.
7. Pushed v2 to GitHub and deployed to gsteam-v2.vercel.app. A push to main deploys.
8. Built the MCP server — 15 tools. Reads roster, rollup, leads, ads, calls board, sync log. Writes metrics, check-ins, events, call notes, leads.
9. It will not add or cancel clients, touch pay, or delete anything.
10. Replaced the master key with per-person sign-in. Postgres enforces the limits now, not my code.
11. Kurt reaches his own book and no further. Remove someone in the app and their access dies the same minute.
12. Wrote a script that proves it by asking Postgres as each person, then rolls back.
13. That check found the calls board was open to anyone with a login, and signups were open. Both closed.
14. It also found v2 could not log metrics at all — the copy left the audit_log sequence behind. Every metric, event and check-in would have failed. Fixed.
15. Built the in-app assistant — a bubble in the corner, one conversation shared by Bobby, Kurt and Mike.
16. Nothing to install and no password to store. They are already signed in.
17. Every write runs as whoever typed it.
18. The MCP and the chat box use one set of tools, not two.
19. Caught the assistant saying "Done" without writing anything, then defending it. Fixed — each reply now carries what it actually did.
20. Fixed the calls board colours in the MCP. It was reading a column the app stopped using in July. It runs the app's own scoring now.

Tomorrow:

1. Build the GoHighLevel and Windsor syncs.
2. Map each client to its GoHighLevel account. None of the 90 has one recorded.

Waiting on Bobby:

1. GoHighLevel agency API key.
2. Windsor.ai key and which ad accounts are connected.
3. Whether Kurt should see the Leads section.
