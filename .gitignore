# Local-only handoff + tooling — never deploy, never commit.
# Per HANDOFF.md: this repo is FLAT (deploy artifact only).
.claude/
GS Team/
_FOR_SLACK/

# Environment files — credentials live here, never in source control.
# .env.example IS committed (template only, no real values).
.env
.env.local
.env.*.local

# Generated by scripts/build-config.js — do not commit.
# Recreated on every Vercel deploy and on `node scripts/build-config.js` locally.
config.js

# Stray nested mirror of src/ — orphan, not used by index.html.
src/src/

# Local-only working docs (status tracker, scratch notes, daily EODs)
PHASES.md
NOTES.md
TODO.md
# docs/ IS tracked in v2 — SPEC-v2.md is the brief, not scratch.

# npm artifacts. v2 has a package.json for the Supabase CLI and tooling.
node_modules/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/
.vercel
.env*
